# AI-АРХИТЕКТУРА ПРОЕКТА

## Воронка точности - Система определения услуг

### Статус: ✅ ЗАВЕРШЕНО (с векторным поиском)

**Назначение:** Определение услуги по текстовому описанию проблемы пользователя.

**Основные компоненты:**
1. **MainAgent** (`main_agent.py`) - главный координатор
2. **TagSearchService** (`tag_search_service.py`) - нечеткий поиск по тегам
3. **SemanticSearchService** (`semantic_search_service.py`) - логико-семантический поиск
4. **VectorSearchService** (`vector_search_service.py`) - векторный поиск
5. **AIAgentService** (`ai_agent_service.py`) - поиск с YandexGPT

**Алгоритм:**
1. Параллельный запуск микросервисов
2. Сбор и фильтрация (порог 70%, дедупликация)
3. Анализ пересечений:
   - **1 кандидат:** SUCCESS → создание заявки
   - **Несколько:** AMBIGUOUS → уточнение
   - **Нет:** AMBIGUOUS → запрос уточнения

---

## VectorSearchService - Векторный поиск

### Реализация: ✅ 2026-01-13

**Двойной векторный поиск:**

1. **По embedding тегов** (точность):
   - Загружает embedding из `ref_tags.embedding_tag` (JSONB, 256 float)
   - Вычисляет косинусное сходство
   - Порог: 0.70
   - Группирует по service_id (максимум)

2. **По embedding услуг** (полнота):
   - Загружает embedding из `services_catalog.embedding_service` (JSONB, 256 float)
   - Вычисляет косинусное сходство
   - Порог: 0.70

3. **Слияние результатов:**
   - Используется **СРЕДНЕВЗВЕШЕННОЕ**: `0.6 * tag_conf + 0.4 * service_conf`
   - Если найден только в одном → не штрафуем (confidence = найденный)
   - Сортировка по DESC, возврат TOP-10

**Формула косинусного сходства:**
```
cosine_sim = (vec1 · vec2) / (||vec1|| * ||vec2||)
где:
- vec1 · vec2 = скалярное произведение
- ||vec|| = L2 норма (длина вектора)
```

**Генерация embedding:**
- Скрипты: `generate_tag_embeddings.py`, `generate_service_embeddings.py`
- Модель: Yandex Embeddings API (`text-search-doc`)
- Размерность: 256 float
- Предобработка: NLTK stopwords (151 слово) + pymorphy2 лемматизация
- Стоимость: ~0.14 руб для всей БД (350 тегов + 68 услуг)

---

## Другие микросервисы

### TagSearchService
- Нечеткий поиск по тегам
- pg_trgm + pymorphy2 + rapidfuzz
- Триграммный индекс в PostgreSQL

### SemanticSearchService
- Логико-семантический поиск
- Правила и маппинги

### AIAgentService
- Поиск с YandexGPT
- Вызовы LLM ТОЛЬКО через этот сервис!

---

## MainAgent - Главный координатор

**Файл:** `main_agent.py`

**Назначение:** Координация всех микросервисов, принятие решения об услуге.

**ПРАВИЛО:** Все вызовы LLM ТОЛЬКО через `AIAgentService`.

**ЗАПРЕЩЕНО:** Прямые HTTP запросы к API, дублирование кода, создание собственных методов `_call_yandex_gpt`

---

## txtPrb - Накопление описания проблемы

**Файл:** `problem_accumulation_service.py`

**Назначение:** Итеративное накопление информации из диалога, извлечение структурированных полей, предотвращение повторяющихся вопросов.

**Принцип:**
- "привет" → '' (нет информации)
- "у меня течет" → 'у пользователя течет'
- "В зале" → 'у пользователя течет в зале'
- "Батарея" → 'у пользователя течет из батареи (отопление) в зале'

**Поля:**
- problem
- location
- source
- category
- severity
- intensity
- object

**Фильтры с confidence:** location, category, incident

**КРИТИЧЕСКИ ВАЖНО:** НЕ спрашивай то, что УЖЕ известно! Если известна локация - НЕ спрашивай "Где именно?"

---

## Запрет на Hardcode

### КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНО:
- ❌ Хардкод списков keywords, правил классификации, маппингов синонимов
- ❌ Вложенные if/else цепочки для классификации текста

**ПРАВИЛЬНЫЙ ПОДХОД:**
- ✅ Использовать LLM (AIAgentService, FilterDetectionService)
- ✅ Хранить данные в БД
- ✅ Использовать векторный поиск (VectorSearchService)
- ✅ Использовать теговый поиск (TagSearchService)

**ИСКЛЮЧЕНИЯ:** Технические константы, stopwords для NLP, временные заглушки с `# TODO: заменить на LLM/БД`
