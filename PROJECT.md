# PROJECT: Kom_Dom_B

## Назначение

Отдельная реализация Коммуналки Б: принять реплику из текстового или
телефонного канала, собрать и подтвердить `адрес/объект + УК + услуга + имя +
контакт + описание проблемы`, затем создать единственный бизнес-документ
`Request` — «Обращение».

## Жёсткая граница с А

- локальная папка: `C:\codex\Kom_Dom_B`;
- production-папка: `/Kom_Dom_B`;
- домен: `aspect.komunal-dom.ru`;
- БД: `komunal_dom_b`, пользователь `komunal_b_app`;
- Git-ветка: `kom-dom-b`;
- служба API: `kom-dom-b-api.service`, порт `127.0.0.1:8100`;
- ключи Redis имеют префикс `kom_dom_b:`;
- проект не импортирует код, документацию, таблицы, очередь или рабочее
  состояние Коммуналки А.

Данные А попадают в Б только через проверяемый импорт. Секреты поставщиков
можно перенести в закрытый `.env`, но токены каналов и внешний API-токен Б
должны стать отдельными до включения трафика.

## Владельцы каталогов

- `www` — FastAPI и веб-портал;
- `max` — MAX-бот и MAX-приложение;
- `tg` — Telegram-транспорт;
- `ai` — этапные промпты, инструменты, оркестратор и редуктор;
- `db` — проект схемы и последовательные SQL-миграции;
- `test` — все автоматические проверки и их временные артефакты;
- `admin` — регламентные задания и эксплуатационные конфигурации;
- `media` — файлы и записи звонков; содержимое не хранится в Git;
- `docs` — утверждённые контракты и инвентаризация.

## Текущее состояние

- отдельная пустая PostgreSQL-БД создана;
- схема v0 спроектирована, но не применена;
- технический FastAPI-контур отдаёт `/health`, `/ready` и страницу входа;
- Telegram-получатель реализован как webhook основного API; до переноса
  бизнес-агента он возвращает честное сообщение о техническом этапе;
- совместимый адрес `/chat/api/external/` существует, но возвращает 503 до
  реализации графа — случайно переключить Asterisk на незавершённый Б нельзя;
- React-интерфейс и бизнес-логика ещё не реализованы.

## Источники решений

1. `AGENTS.md` — правила проекта и запрет смешения с А.
2. `docs/ARCHITECTURE.md` — минимальный устойчивый диалог.
3. `docs/DB_SCHEMA.md` и `db/design/schema_v0.sql` — модель данных.
4. `docs/API_SERVICE_INVENTORY.md` — что есть в А и какие отдельные службы/URL
   принадлежат Б.
5. `docs/AI_AGENT_RAG.md` — этапные промпты, инструменты и RAG.
6. `docs/CATALOG_SEARCH_DECISION.md` — решение по векторам и Redis.
7. `docs/DEPLOYMENT.md` — текущий сервер, env, Nginx, БД и проверки.
8. `DEPLOY_NEW_SERVICE.md` — воспроизводимое развёртывание на новом сервере.
9. `docs/UI_INVENTORY.md` — интерфейсы, включая отладчик хода в стиле n8n.

## Следующая продуктовая задача

Перенести `request_types` отдельной первой миграцией, затем категории/услуги,
реализовать восемь шагов диалога и только после приёмки разрешить бизнес-API.
